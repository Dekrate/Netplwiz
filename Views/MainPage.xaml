<?xml version="1.0" encoding="UTF-8" ?>
<Page
    x:Class="Netplwiz.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:Netplwiz.ViewModels"
    mc:Ignorable="d">

    <Page.DataContext>
        <vm:MainViewModel />
    </Page.DataContext>

    <Grid Padding="0,48,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Title + About + Settings -->
        <Grid Grid.Row="0" Margin="24,16,24,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock
                Grid.Column="0"
                VerticalAlignment="Center"
                FontSize="20"
                FontWeight="SemiBold"
                x:Uid="AppTitle" />
            <Button
                Grid.Column="1"
                Margin="0,0,8,0"
                VerticalAlignment="Center"
                Click="SettingsButton_Click"
                x:Uid="SettingsButton" />
            <Button
                Grid.Column="2"
                VerticalAlignment="Center"
                Click="AboutButton_Click"
                x:Uid="AboutButton" />
        </Grid>

        <!-- Navigation -->
        <NavigationView
            x:Name="MainNavigation"
            Grid.Row="1"
            PaneDisplayMode="Top"
            IsBackButtonVisible="Collapsed"
            IsSettingsVisible="False"
            SelectionChanged="MainNavigation_SelectionChanged">
            <NavigationView.MenuItems>
                <NavigationViewItem x:Uid="UsersTab" x:Name="UsersNavItem" />
                <NavigationViewItem x:Uid="AdvancedTab" x:Name="AdvancedNavItem" />
            </NavigationView.MenuItems>
            <Grid x:Name="ContentRoot" Padding="12,0,12,12" />
        </NavigationView>

        <!-- Status Bar -->
        <Grid Grid.Row="2" Padding="12,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock
                Grid.Column="0"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Text="{Binding StatusMessage}" />
            <ProgressRing
                Grid.Column="1"
                Width="16"
                Height="16"
                IsActive="{Binding IsLoading}" />
        </Grid>
    </Grid>
</Page>
