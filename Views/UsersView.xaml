<?xml version="1.0" encoding="UTF-8" ?>
<UserControl
    x:Class="Netplwiz.Views.UsersView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="12" RowSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            x:Uid="UsersDescription"
            TextWrapping="Wrap" />

        <TextBlock
            Grid.Row="1"
            FontWeight="SemiBold"
            x:Uid="UsersLabel" />

        <ListView
            x:Name="UsersListView"
            Grid.Row="2"
            Margin="0,4,0,8"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            ItemsSource="{Binding Users}"
            SelectedItem="{Binding SelectedUser, Mode=TwoWay}"
            SelectionMode="Single">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="32" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <FontIcon
                            Grid.Column="0"
                            VerticalAlignment="Center"
                            Glyph="&#xE77B;" />
                        <TextBlock
                            Grid.Column="1"
                            VerticalAlignment="Center"
                            Text="{Binding UserName}" />
                        <TextBlock
                            Grid.Column="2"
                            VerticalAlignment="Center"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            Text="{Binding GroupsDisplay}"
                            TextTrimming="CharacterEllipsis" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <StackPanel
            Grid.Row="3"
            Margin="0,4,0,8"
            HorizontalAlignment="Center"
            Orientation="Horizontal"
            Spacing="8">
            <DropDownButton x:Uid="AddButton">
                <DropDownButton.Flyout>
                    <MenuFlyout>
                        <MenuFlyoutItem Text="Dodaj użytkownika lokalnego..." Click="AddLocalUser_Click" />
                        <MenuFlyoutItem Text="Dodaj przez Ustawienia Windows..." Click="AddViaSettings_Click" />
                    </MenuFlyout>
                </DropDownButton.Flyout>
            </DropDownButton>
            <Button
                Command="{Binding RemoveUserCommand}"
                x:Uid="RemoveButton" />
            <Button
                Command="{Binding PropertiesCommand}"
                x:Uid="PropertiesButton" />
            <Button
                Command="{Binding UserDetailsCommand}"
                x:Uid="UserDetailsButton" />
        </StackPanel>

        <!-- Password section for selected user -->
        <Border
            Grid.Row="4"
            Padding="12"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="8"
            Visibility="{Binding SelectedUser, Converter={StaticResource ObjectToVisibilityConverter}}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <FontIcon
                    Grid.Row="0"
                    Grid.RowSpan="2"
                    Grid.Column="0"
                    Margin="0,0,12,0"
                    VerticalAlignment="Center"
                    FontSize="24"
                    Glyph="&#xE77B;" />

                <TextBlock
                    Grid.Row="0"
                    Grid.Column="1"
                    FontWeight="SemiBold">
                    <Run Text="Hasło dla " />
                    <Run Text="{Binding SelectedUser.UserName}" />
                </TextBlock>

                <TextBlock
                    Grid.Row="1"
                    Grid.Column="1"
                    Margin="0,4,0,0"
                    x:Uid="PasswordSectionDescription"
                    TextWrapping="Wrap" />

                <Button
                    Grid.Row="1"
                    Grid.Column="1"
                    Margin="0,8,0,0"
                    HorizontalAlignment="Right"
                    Command="{Binding ResetPasswordCommand}"
                    x:Uid="ResetPasswordButton" />
            </Grid>
        </Border>
    </Grid>
</UserControl>
